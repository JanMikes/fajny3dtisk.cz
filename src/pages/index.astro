---
import PageTemplate from '../components/PageTemplate.astro';
import { languages } from '../i18n/config';

const { translations: t, url, path } = languages.cs;
---

<PageTemplate t={t} url={url} path={path} />
